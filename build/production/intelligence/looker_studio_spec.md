# Looker Studio Executive Dashboard Architecture

## 1. Total Fan Throughput (Ingress Velocity)
**Logic Path**: Time-Series count validating distinct `device_id` executions across natively classified `Gate` boundaries rolling on a 60-second execution window.
**Visualization Element**: Time-Series Line Chart natively overlaying historical match ingress metrics validating baseline security checking efficiencies globally.

## 2. Revenue Correlation (The Operational Velocity Hypothesis)
**Logic Path**: Integrate and `JOIN` standard concession POS (Point of Sale) revenue streams mapping cleanly against Pub/Sub generated wait-times natively. 
**Objective**: Explicitly confirm the hypothesis that bounding average concession line times directly maps to a quantifiable increase in transactional revenue arrays (e.g. 5m wait -> $4.5k/hr; 18m wait -> $2.1k/hr).
**Visualization Element**: Dual-Axis Scatter Plot displaying Sales Revenue metrics explicitly correlated against mathematical Wait Averages natively per concession zone.

## 3. Crowd Safety Score (Congestion Boundary Metrics)
**Logic Path**: Aggregate duration sums natively where discrete Zones explicitly operate strictly above `80%` native capacity constraints tracking continuous interval metrics natively.
**Visualization Element**: Structural Stadium Topology Heatmap overlaying zones dynamically rendering explicitly red natively tracking sectors holding `>80% capacity` spanning continuously past 15-minute standard thresholds.
